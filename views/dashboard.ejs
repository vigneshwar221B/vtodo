<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">

    <% newListItems.forEach(function(singleItem){ %>
    
    <form action="/users/dashboard/delete" method="post">
      <div class="item">
      
          <input type="checkbox" value = "<%= singleItem._id %>" name = "checkbox" onChange = "this.form.submit()">
            <p> <%=  singleItem.name  %> </p>
    
        </div>
        <input type= "hidden" name = "listName" value = "<%= listTitle %>"></input>
    </form>

    <% }); %>
      

        <form class="item" action="/users/dashboard" method="post">
          <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
          <button type="submit" name="list" value="<%= listTitle %>">+</button>
        </form>
  </div>

<%- include("footer") -%>
